projects:
- name: dev
  dir: .
  workspace: default
  terragrunt: true

workflows:
  default:
    steps:
      - plan: terragrunt run-all plan --terragrunt-log-level debug --terragrunt-debug
      - apply: terragrunt run-all apply
